# Repo Cleanup 2026-05-27-6

This archive bucket stores inactive runtime byproducts moved out of the active
workspace during the repository cleanup pass.

Moved items:

- `.pytest_cache/` was moved to `runtime-cache/pytest-cache/`.
- 94 Python `__pycache__/` directories from `src/`, `tests/`, and
  `appgen-frontend/` search scopes were moved under
  `runtime-cache/python-bytecode/` while preserving their original paths.
- 94 regenerated Python `__pycache__/` directories from the post-move
  verification probe were moved under
  `runtime-cache-post-verify/python-bytecode/`.

Active source, documentation, tests, frontend files, package metadata, and the
local virtual environment were left in place.

Verification:

- Confirmed no `__pycache__/` directories remain under active `src/`, `tests/`,
  or `appgen-frontend/` paths immediately after the move.
- Confirmed no active-tree `.pytest_cache/` directory remains at repository
  depth two.
- Ran a no-bytecode environment import probe for visual readiness and PBC release
  audit; both returned true. The audit path still regenerated bytecode, which was
  swept into `runtime-cache-post-verify/`.
