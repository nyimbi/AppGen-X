# Repo Cleanup Archive - 2026-05-27 Pass 4

This archive captures generated runtime cache artifacts that were present in
the active repository tree but are not source, documentation, tests, or build
configuration.

## Archived Payload

- `.pytest_cache/`
- `src/**/__pycache__/`
- `tests/**/__pycache__/`
- `src/**/__pycache__/` and `tests/**/__pycache__/` regenerated during
  status/inspection and were moved again into `runtime-cache-second-sweep/`.
- Import verification regenerated cache directories again; those payloads were
  moved into `runtime-cache-post-verify/` and `runtime-cache-final-sweep/`.

## Counts

- Active `.pytest_cache/` directories moved: 1
- Active Python cache directories moved from `src/` and `tests/`: 96
- Archived runtime cache files: 915
- Second-sweep active Python cache directories moved from `src/` and `tests/`: 96
- Second-sweep archived runtime cache files: 844
- Post-verification active Python cache directories moved from `src/` and `tests/`: 96
- Post-verification archived runtime cache files: 908
- Final-sweep active Python cache directories moved from `src/` and `tests/`: 92
- Final-sweep archived runtime cache files: 782

## Restore Notes

These files are generated by Python and pytest. They are intentionally ignored
inside this archive bucket and should not be restored unless a future debugging
session needs the exact local cache state from this pass.
