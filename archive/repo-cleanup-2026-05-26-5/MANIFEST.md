# Repo Cleanup Archive 2026-05-26 5

## Scope

This archive stores generated runtime and test cache artifacts moved out of the
active source tree during the repository cleanup pass requested on 2026-05-26.

## Cleanup Plan

1. Archive only clearly generated, ignored runtime debris: root `.pytest_cache`
   and active `__pycache__` folders under `src/` and `tests/`.
2. Preserve moved payloads under `runtime-cache/` so the cleanup is reversible.
3. Keep `.venv`, tracked source, documentation, configuration, tests, and
   existing archive history in place because they were not proven unused.

## Archived Items

- `runtime-cache/pytest-cache/`: root pytest cache.
- `runtime-cache/src_pyAppGen___pycache__/`: package import cache directory.
- `runtime-cache/src_pyAppGen_dsl_generated_lang___pycache__/`: generated DSL
  parser import cache directory.
- `runtime-cache/src_pyAppGen_pbcs_*___pycache__/`: generated PBC package import
  cache directories present at cleanup time.
- `runtime-cache/tests___pycache__/`: test import cache directory.
- `post-verify-cache/`: import cache directories regenerated by the focused
  verification run and swept out of the active tree afterward.

The `runtime-cache/` payload is ignored so generated bytecode and test-cache
files are not added to repository history. This manifest records the cleanup
scope, boundaries, and verification evidence.

## Verification

- Active cache scan after the move found no `.pytest_cache` or `__pycache__`
  directories outside `.venv` and `archive/`.
- Focused source/package syntax check passed:
  `/usr/bin/env PYTHONDONTWRITEBYTECODE=1 python3 -B -m py_compile src/pyAppGen/pbc.py tests/test_pbc_source_packages.py`.
- Focused PBC regression check passed:
  `/usr/bin/env PYTHONDONTWRITEBYTECODE=1 .venv/bin/python -B -m pytest -p no:cacheprovider tests/test_pbc_source_packages.py tests/test_pbc_implemented_capability_audit.py`
  (`4 passed`).
- Whitespace check passed: `git diff --check`.
- Restricted-name scan over touched cleanup docs/manifests had no hits.
