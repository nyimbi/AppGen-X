# Repo Cleanup Archive 2026-05-26 12

This archive records a conservative cleanup pass that moved live Python bytecode
cache directories out of the active source and test trees.

## Archived

- `src/pyAppGen/**/__pycache__/`
- `src/pyAppGen/pbcs/**/__pycache__/`
- `src/pyAppGen/pbcs/**/tests/__pycache__/`
- `tests/__pycache__/`

The initial payload is stored under `runtime-cache/` with path separators
converted to double underscores. Verification regenerated bytecode caches, so
those follow-up payloads were moved under
`runtime-cache-regenerated-after-verify/` and `runtime-cache-final-pass/`.
Runtime cache contents remain ignored because they are generated interpreter
artifacts, but this manifest and `.gitkeep` files preserve the archive
locations and cleanup audit trail.

## Restore

These files are generated cache artifacts. If a cache payload is ever needed,
copy the matching directory from `runtime-cache/` back to its original path and
replace double underscores with path separators.

## Verification

- Confirmed no tracked source/docs/config files were moved in this pass.
- Confirmed root build, documentation, and packaging files still have active
  references and were not archived.
- Final archive move reported zero active `__pycache__` directories under
  `src/` and `tests/`.
