# Runtime Cache Cleanup - 2026-05-26

This archive pass moved reproducible local runtime artifacts out of active source
and test paths without changing application code.

## Moved

- `.pytest_cache/`
- `tests/__pycache__/`
- `src/pyAppGen/**/__pycache__/`

## Summary

- Archived cache roots: `archive/runtime-cache-cleanup-2026-05-26-2/cache/`
- Python bytecode cache directories moved: 50
- Archived files: 158
- Approximate archived size: 8.9M

## Rationale

These files are generated by local Python and pytest runs, are ignored by git,
and can be recreated by running the test suite or importing the package. Keeping
them out of active source directories makes repository inspection cleaner while
preserving a recoverable copy under `archive/`.

## Verification

- Behavior lock before move: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src ./.venv/bin/python -c "from pyAppGen.schema import AppSchema; import pyAppGen.gen as gen; print('behavior lock import ok')"`
- Post-move check: no `.pytest_cache` or `__pycache__` directories remain outside
  `.venv`, `.git`, or `archive`.
