# Repo Cleanup Archive - 2026-05-27

## Scope

Archived ignored runtime/test cache artifacts from the active repository tree:

- `.pytest_cache/`
- `src/**/__pycache__/`
- `tests/**/__pycache__/`

The move preserved source-relative paths under:

```text
archive/repo-cleanup-2026-05-27/runtime-cache/
```

## Rationale

These directories are generated by Python compilation and test execution. They
are not source, documentation, templates, package manifests, or platform
contracts.

## Evidence

- Candidate count before move: 97 directories.
- Active tree cache scan after move: no `.pytest_cache/` or `__pycache__/`
  directories outside `archive/`.
- Existing source files and in-flight PBC release-evidence edits were not
  modified by this cleanup.

## Notes

The archived cache payload is intentionally ignored by git; this manifest is
the durable record of what was moved and why.
