# Repo Cleanup Archive 2026-05-26 4

## Scope

This archive stores generated runtime and test cache artifacts moved out of
the active source tree during the repository cleanup pass.

## Cleanup Plan

1. Archive ignored runtime debris only: `.pytest_cache` and active
   `__pycache__` trees under `src/` and `tests/`.
2. Preserve cache contents under flattened archive directory names so the
   active tree no longer contains live cache folders.
3. Leave `.venv`, tracked source, package configuration, docs, tests, and
   unrelated dirty PBC implementation work in place.

## Evidence

- `.gitignore` excludes `.pytest_cache/`, `__pycache__/`, and `/.venv/`.
- `git status --ignored --short` showed live ignored cache directories under
  `src/pyAppGen`, `src/pyAppGen/pbcs`, and `tests`.
- `.venv` was intentionally not moved because it may be the local execution
  environment used for validation.
- No tracked source or configuration files were archived in this pass.

## Archived Items

- `runtime-cache/pytest-cache/`: root pytest cache.
- `runtime-cache/src__pyAppGen____pycache__/`: package import cache.
- `runtime-cache/src__pyAppGen__pbcs____pycache__/`: PBC package import cache.
- `runtime-cache/src__pyAppGen__pbcs__*____pycache__/`: PBC module import
  caches present at cleanup time.
- `runtime-cache/tests____pycache__/`: test import cache.
- `post-verify-runtime-cache/`: cache artifacts regenerated by the import and
  focused test validation commands.
- `final-runtime-cache/`: final cache sweep after validation and status checks.
- `final-runtime-cache-2/`: final observed cache regeneration sweep.

The cache payload folders are ignored in this archive so generated bytecode is
not added to repository history; this manifest records the cleanup action and
evidence.

## Verification

- Source import smoke: `python3 -B -c "import sys; sys.path.insert(0, 'src'); import pyAppGen; import pyAppGen.pbc; print('import ok')"` passed.
- Focused regression: `.venv/bin/python -B -m pytest -p no:cacheprovider tests/test_pbc_ap_automation_runtime.py` passed (`3 passed`).
- Whitespace: `git diff --check` passed.
- Restricted-name scan over touched docs/manifests had no hits.
- Active cache scan passed after the final sweep: no `.pytest_cache` or
  `__pycache__` directories remain outside `.venv` and `archive`.
