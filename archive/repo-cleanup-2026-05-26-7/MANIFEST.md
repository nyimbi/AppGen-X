# Repo Cleanup Archive - 2026-05-26 7

This archive records verification debris moved out of the active source tree
after the native runtime authoring replay and PBC specification verification
pass.

Moved out of the active tree:

- `.pytest_cache/`
- `src/**/__pycache__/`
- `tests/**/__pycache__/`

The `post-pbc-verify-cache/` subdirectory contains the same generated cache
classes after the focused PBC source package verification rerun.

The `post-commit-cache/` subdirectory contains the regenerated cache classes
after the final compile verification and split commits.

The runtime cache payload is generated bytecode/test metadata and is
intentionally ignored. The manifest is tracked so the cleanup decision remains
auditable.
