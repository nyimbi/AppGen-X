# Repo Cleanup 2026-05-27-7

This archive bucket stores runtime byproducts regenerated by focused effect
editor verification.

Moved items:

- 96 Python `__pycache__/` directories from `src/`, `tests/`, and
  `appgen-frontend/` search scopes were moved under
  `runtime-cache-after-effect-editor-verify/` while preserving original paths.

Active source, documentation, tests, frontend source, package metadata, and the
local virtual environment were left in place.
